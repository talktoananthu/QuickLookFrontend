import { HourMinutePipe } from './hour-minute.pipe';

describe('HourMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new HourMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
